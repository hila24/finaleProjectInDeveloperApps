# SnapVote — מה יש לנו בפרויקט

> העלו · דרגו · היעלמו

אפליקציית אנדרואיד לסקרי תמונות בין חברים. מעלים כמה תמונות, קובעים מועד סיום,
והחברים מצביעים או מדרגים. התוצאות מתעדכנות בזמן אמת, ובתום הסקר התמונות נמחקות
אוטומטית ונשארות רק התוצאות.

הקובץ הזה מרכז מה בדיוק נבנה, איך זה עובד, ומה כדאי לדעת להסביר.

| | |
|---|---|
| מסכים | 9 |
| שורות Kotlin | 3,196 |
| בדיקות עוברות | 55 |
| שירותי Firebase | 4 |
| עלות תפעול | ₪0 |

---

## המסע באפליקציה

שישה שלבים לפי הסדר שבו משתמשת פוגשת אותם, עם הקובץ שאחראי על כל אחד — שימושי
אם שואלים "איפה זה בקוד".

### 1. הרשמה והתחברות
אימייל וסיסמה דרך Firebase Authentication. שם המשתמש חייב להיות ייחודי — זה מה
שמאפשר לחברות למצוא אותך בלי לדעת את האימייל.
`ui/auth/LoginFragment.kt` · `RegisterFragment.kt`

### 2. הוספת חברים
חיפוש לפי שם משתמש והוספה מיידית. החברות הדדית — הוספה כותבת רשומה בשתי רשימות
החברים בבת אחת.
`ui/friends/FriendsFragment.kt`

### 3. יצירת סקר
עד 6 תמונות מהגלריה או מהמצלמה, מועד סיום, ובחירה בין הצבעה לתמונה אחת לבין דירוג
בכוכבים. כל תמונה נדחסת פעמיים — ממוזערת לפיד ומלאה למסך ההצבעה.
`ui/create/CreatePollFragment.kt`

### 4. הפיד והצבעה
הפיד מציג רק סקרים של חברים, ממוין לפי מי שנסגר הכי מוקדם, ומתעדכן חי בלי רענון.
אפשר להצביע פעם אחת בלבד — וזה נאכף בשרת, לא רק במסך.
`ui/feed/FeedFragment.kt` · `ui/vote/VoteFragment.kt`

### 5. תוצאות, תגובות ומי הצביע
אחוזים שזזים בזמן אמת, תמונה מנצחת, ומקטע תגובות. יוצרת הסקר — ורק היא — רואה מי
הצביעה ובמה בחרה.
`ui/results/ResultsFragment.kt` · `ui/voters/VotersFragment.kt`

### 6. היעלמות
כשהמועד מגיע התמונות נמחקות — גם המלאות וגם הממוזערות — והתוצאות נשארות. במקביל
נשלחת התראה מקומית שהסקר נסגר.
`data/repository/PollRepository.kt` · `work/PollDeadlineWorker.kt`

---

## איך זה בנוי

ארבע שכבות, כל אחת מדברת רק עם זו שמתחתיה. המבנה הזה הוא הסיבה שאפשר לבדוק את
לוגיקת החישוב בלי אנדרואיד ובלי Firebase.

```
Fragment      מסך: מציג ומקבל לחיצות. לא יודע דבר על Firebase   [ViewBinding]
   ↓
ViewModel     מחזיק מצב, שורד סיבוב מסך, מתרגם שגיאות לעברית    [LiveData]
   ↓
Repository    המקום היחיד שמכיר את מבנה מסד הנתונים             [Coroutines · Flow]
   ↓
Firebase      Authentication · Firestore · Security Rules · Hosting
```

### מחלקות בסיס משותפות
כל תשעת המסכים יורשים מ‑`BaseFragment`, שמרכז את מה שחזר בכולם: יצירת ה‑binding
ושחרורו, הצגת הודעה, וניווט בטוח. ארבעת ה‑Adapter שתלויים בנתון חיצוני יורשים
מ‑`RefreshableListAdapter`, שדוחה רענון של הרשימה אם היא באמצע פריסה — קריאה
לרענון באותו רגע זורקת חריגה.

### ניווט
Activity אחד בלבד, וכל המסכים הם Fragment בתוך גרף ניווט אחד
(`res/navigation/nav_graph.xml`). זה מה שמאפשר שקישור מוואטסאפ ייפול ישירות על מסך
ההצבעה.

### למה התמונות נשמרות ב‑Firestore
פרויקטים חדשים ב‑Firebase דורשים תוכנית בתשלום כדי להשתמש ב‑Cloud Storage. כדי
שהפרויקט יעבוד לגמרי בחינם, כל תמונה נשמרת כטקסט Base64: ממוזערת של כ‑12KB יושבת
על מסמך הסקר — כך שהפיד עולה בקריאה אחת — והתמונה המלאה, עד 700KB, במסמך נפרד
מתחת למגבלת ה‑1MB של Firestore.

---

## מודל הנתונים

חמישה אוספים ב‑Cloud Firestore. הקינון נבחר כך שכל מסך יקרא כמה שפחות מסמכים.

```
// טבלת ייחודיות – כך מוצאים חברה לפי שם
usernames/{lowercase name}
    uid, username

users/{uid}
    username, usernameLower, email, createdAt,
    pollsCreated, votesGiven

users/{uid}/friends/{friendUid}        // חברות הדדית
    username, since

polls/{pollId}
    question, ownerId, ownerName, mode,
    createdAt, deadline, voteCount,
    tally: { imageId: score },
    visibleTo: [uid, ...]              // מי רשאי לראות
    imagesDeleted,
    images: [{ id, label, thumb }]     // ממוזערות

polls/{pollId}/images/{imageId}
    data                               // התמונה המלאה, Base64

polls/{pollId}/votes/{uid}
    userName, choiceImageId, ratings, createdAt

polls/{pollId}/comments/{commentId}
    userId, userName, text, createdAt
```

**למה `tally` יושב על מסמך הסקר ולא מחושב מההצבעות?** כדי שהפיד ומסך התוצאות יציגו
מספרים בלי לקרוא את כל אוסף ההצבעות. הספירה מתעדכנת אטומית בכל הצבעה.

---

## אבטחה ופרטיות

כל כלל נאכף בשרת ב‑`firestore.rules`, לא במסך. גם מי שיפנה ישירות למסד הנתונים,
בלי לעבור דרך האפליקציה, ייחסם.

| מה מוגן | איך |
|---|---|
| סקר נראה רק לחברים | בכל סקר נשמר `visibleTo`, והכלל דורש שהמשתמש יהיה בתוכו |
| אי אפשר להצביע פעמיים | מסמך הצבעה אחד לכל משתמש, ועדכון שלו אסור לחלוטין |
| אי אפשר להצביע אחרי הדדליין | הכלל משווה את מועד הסיום לשעון השרת, לא לשעון הטלפון |
| מצביע לא יכול לשנות את הסקר | למי שאינו הבעלים מותר לגעת רק בשדות הספירה |
| "מי הצביע" ליוצרת בלבד | קריאת ההצבעות מותרת לבעלת הסקר, או למצביעה על ההצבעה שלה בלבד |
| שם משתמש לא נגנב | מסמך שם אפשר ליצור פעם אחת ולא לדרוס |

### ספירה בטוחה בהצבעה בו־זמנית
אם שתי חברות מצביעות באותה שנייה, קריאה ועדכון רגילים היו גורמים לאחת לדרוס את
השנייה. ההצבעה רצה בתוך `runTransaction` עם `FieldValue.increment` — הפעולה
מתבצעת בצד השרת, וכל קול נספר.

---

## מה נבדק

55 בדיקות אוטומטיות שעוברות. חלקן על הלוגיקה בקוד, וחלקן מול Firebase האמיתי —
אלה בודקות שהכללים באמת חוסמים ולא רק "אמורים" לחסום.

| סוג | מה נבדק | תוצאה |
|---|---|---|
| בדיקות יחידה | מנצחת, אחוזים ועיגולם, ממוצע דירוג, מצב הדדליין | 14 / 14 |
| זרימה מלאה | הרשמה, יצירת סקר, הצבעה, ספירה, חסימת הצבעה כפולה | 14 / 14 |
| חברים | לא‑חברה חסומה מקריאה ומהצבעה, שאילתת הפיד, ייחודיות שם | 13 / 13 |
| מי הצביע | מי רשאית לקרוא את ההצבעות ומי לא | 6 / 6 |
| תגובות | התחזות, תגובה ריקה, מחיקה, הרשאות זרים | 8 / 8 |

הרצת בדיקות היחידה:

```bash
./gradlew :app:testDebugUnitTest
```

**מה עדיין לא נבדק:** כל מה שדורש מכשיר אמיתי — פתיחת קישור מוואטסאפ, התראות,
וסנכרון בין שני טלפונים. הרשימה המלאה ב‑[BEDIKOT.md](BEDIKOT.md).

---

## שאלות שעלולות לצוץ

תשובות מוכנות, במיוחד למקומות שבהם המימוש שונה מהמצגת המקורית.

**למה התמונות ב‑Firestore ולא ב‑Cloud Storage?**
כי פרויקט Firebase חדש דורש תוכנית בתשלום כדי להפעיל Cloud Storage. הפתרון: דחיסה
ל‑JPEG ושמירה כ‑Base64 — ממוזערת על מסמך הסקר, מלאה במסמך נפרד. תמונה טיפוסית
יורדת מכ‑4MB לכ‑150KB.

**איך מונעים הצבעה כפולה?**
לכל משתמשת יש בדיוק מסמך הצבעה אחד, שמזהה שלו הוא ה‑uid שלה. כלל האבטחה מתיר
יצירה בלבד ואוסר עדכון, כך שגם ניסיון ישיר מול מסד הנתונים נכשל.

**מה קורה כששתי חברות מצביעות בדיוק באותו רגע?**
ההצבעה רצה בטרנזקציה, והספירה מתעדכנת בפעולת increment שמתבצעת בשרת. אף קול לא
נדרס.

**למה המחיקה האוטומטית לא רצה ב‑Cloud Function מתוזמן?**
פונקציות מתוזמנות דורשות תוכנית בתשלום. במקום זה המחיקה מתבצעת מהאפליקציה:
כשבעלת הסקר פותחת אותה, כל סקר שלה שהמועד שלו עבר מנוקה — התמונות נמחקות
והתוצאות נשארות. המשמעות: המחיקה תלויה בכך שהבעלים ייפתחו את האפליקציה. זו פשרה
מודעת, והמעבר לפונקציה מתוזמנת הוא שינוי בקובץ אחד.

**למה חברה שהוספתי היום לא רואה סקר שיצרתי אתמול?**
כי `visibleTo` נקבע ברגע יצירת הסקר — כמו פוסט שפורסם לקהל מסוים. זו בחירה
מכוונת: היא מאפשרת לאכוף את הפרטיות בשאילתה אחת ובכלל אחד, בלי לבדוק את רשימת
החברים מחדש בכל קריאה.

**איך קישור מוואטסאפ פותח את האפליקציה?**
הקישור הוא כתובת https אמיתית, ולכן וואטסאפ הופך אותה ללחיצה. באתר יושב קובץ
אימות שמצהיר שהדומיין והאפליקציה שייכים לאותו מפתח, ואנדרואיד מכבד את ההצהרה
ופותח את האפליקציה במקום הדפדפן. מי שהאפליקציה לא מותקנת אצלה מגיעה לדף נחיתה.

**איך ההתראות עובדות בלי שרת?**
הן מקומיות. ביצירת סקר נקבעות שתי משימות WorkManager — אחת שעה לפני הסגירה ואחת
בסגירה — שרצות על המכשיר עצמו. התראות שרת היו דורשות Cloud Functions, כלומר תשלום.

---

## מה נשאר

* לעבור על [BEDIKOT.md](BEDIKOT.md) עם הטלפון — במיוחד קישור מוואטסאפ והתראות
* להשלים את העברת הבעלות על פרויקט Firebase לחשבון הפרטי
* להכין חשבון שני עם חברה מוספת וסקר פעיל, כדי שההדגמה לא תתחיל בפיד ריק
* לבדוק מול המרצה מה נדרש בסרטון ההגשה
